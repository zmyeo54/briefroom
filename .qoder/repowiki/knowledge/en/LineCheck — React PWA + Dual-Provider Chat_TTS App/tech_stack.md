React 19 + Vite 8 + Tailwind CSS v4 (`@tailwindcss/vite`); routing via `react-router-dom` v7; animations via `framer-motion`; LLM dual-provider via OpenAI-compatible chat endpoint against Google Gemini and DeepSeek; TTS via Microsoft Edge TTS (`msedge-tts`); PDF export via `html2pdf.js` + `pdfjs-dist`; OCR via `tesseract.js`; Playwright + Puppeteer stealth used in regression scripts.