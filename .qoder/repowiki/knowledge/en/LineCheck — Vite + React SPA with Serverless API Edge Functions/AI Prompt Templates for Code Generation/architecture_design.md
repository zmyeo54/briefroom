Flat collection of YAML-frontmatted `.prompt` files consumed by an external AI coding assistant (e.g. Cursor/Copilot). Each file declares a `name` and `description` frontmatter block followed by free-text instructions; there is no runtime code, build step, or internal dependency graph — the module's only boundary is the filename convention (`*.prompt`) that the host tooling indexes.